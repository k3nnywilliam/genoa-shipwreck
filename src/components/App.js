import React, { Component } from 'react';
import LidarViewer from './Viewer.js';

export default class App extends Component {
    render() {
        return (
            <LidarViewer/>
        );
    }
}
